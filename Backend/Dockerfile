# VERSION DEL NODE
FROM node:22-alpine
# DIRECTORIO DE TRABAJO
WORKDIR /app
# COPIAR PACKAGE.JSON
COPY package*.json ./
# INSTALAR DEPENDENCIAS
RUN npm install
# COPIAR DEPENDENCIAS
COPY . .
# PUERTO
EXPOSE 3000
# INICIAR APP
CMD [ "npm", "start" ]
